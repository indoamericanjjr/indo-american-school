# TODO

- Please delete the `vite.config.js` file. It was conflicting with `vite.config.ts` and was preventing the application from running correctly. I have merged the necessary configurations into `vite.config.ts`.
- I have fixed the `HeroSlider.tsx` component to preload the images, this will improve the user experience.
- I have added the missing `useRef` import to the `HeroSlider.tsx` component.
- I have fixed the `Vite` config to include the `react` plugin.
- I have updated the `eslint.config.js` file to include the `react` plugin.
